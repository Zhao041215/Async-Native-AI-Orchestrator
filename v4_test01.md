# 简易人员信息管理系统 - 需求规格说明书

## 1. 项目概述

### 1.1 项目名称
简易人员信息管理系统

### 1.2 技术栈
- 后端：PHP
- 数据库：MySQL
- 前端：原生 HTML + CSS + 少量 JavaScript（可引入 Bootstrap 美化）
- 代码规模：约 1000 行

### 1.3 核心目标
打造一个轻量级的 Web 应用，实现对机构内部人员信息的集中管理。核心逻辑围绕"增删改查"展开，代码结构力求简洁清晰，适合作为部门内部工具或课程设计项目。

---

## 2. 用户角色

系统仅设置两类用户，权限分明：

| 角色 | 权限说明 |
|------|----------|
| 管理员 | 可执行所有操作，包括增删改查人员，以及管理系统账号 |
| 普通用户（访客） | 仅可浏览人员列表和搜索，无法修改或删除 |

---

## 3. 功能模块

### 3.1 模块一：用户认证

- **登录**：验证用户名密码，成功则跳转首页，失败则提示错误信息
- **会话控制**：利用 PHP Session 保持登录状态
- **退出登录**：清除 Session 并跳转回登录页

### 3.2 模块二：人员信息管理（核心）

人员信息包含以下字段：

| 字段类别 | 字段名 | 约束 |
|----------|--------|------|
| 基础信息 | 姓名 | 必填 |
| 基础信息 | 工号 | 必填、唯一 |
| 基础信息 | 性别 | 男 / 女 |
| 基础信息 | 部门 | - |
| 基础信息 | 职位 | - |
| 基础信息 | 手机号 | 格式校验 |
| 基础信息 | 邮箱 | 格式校验 |
| 状态信息 | 入职日期 | 日期格式 |
| 状态信息 | 在职状态 | 在职 / 离职 |

功能点：

- **列表展示**：以表格形式分页展示所有人员，支持按部门、姓名或工号进行模糊搜索
- **新增人员**：通过表单提交，需做基础验证（工号不可重复、手机号格式等）
- **编辑人员**：回填已有数据，修改后保存
- **删除人员**：删除前弹出确认框，确认后物理删除记录

### 3.3 模块三：简易仪表盘

首页展示关键统计数字：

- 人员总数
- 在职人数
- 部门数量



## 5. 操作流程

1. **访问系统**：自动跳转至登录页
2. **登录鉴权**：验证通过 → 进入首页仪表盘；失败 → 留在当前页并提示错误
3. **首页仪表盘**：查看统计卡片，通过侧边栏或导航菜单切换功能
4. **人员管理列表**：浏览人员表格，顶部提供搜索框和"新增人员"按钮
5. **新增 / 编辑**：弹出模态框或跳转至表单页，提交后返回列表并显示操作反馈
6. **删除**：点击"删除"按钮，弹出确认框，确认后记录移除
7. **退出登录**：点击右上角退出按钮，返回登录页

---

## 6. 界面与体验要求

- **技术实现**：不依赖复杂前端框架，使用原生 HTML + CSS + 少量原生 JS；可选引入 Bootstrap 快速美化
- **布局风格**：采用后台管理常见的侧边栏布局
- **响应式**：对移动端做基本适配
- **操作反馈**：操作成功或失败时，需有明确的文字提示（如绿色成功提示 / 红色错误提示）

---

## 7. 安全与校验

| 安全项 | 实现要求 |
|--------|----------|
| 密码加密 | 必须使用 `password_hash()` 加密存储，`password_verify()` 验证 |
| SQL 注入防护 | 统一使用 PDO 预处理语句 |
| 权限控制 | 每个受保护的后端页面开头，需检查 Session 是否存在 |
| 输入校验 | 前端使用 HTML5 属性（`required`、`pattern` 等）做基础校验；后端必须再次校验，防止空数据提交、重复工号等 |

---

## 8. 推荐文件结构
